{
  "name": "my-timer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "vitest run",
    "check": "npm run test && npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "vitest": "^4.1.4"
  }
}
